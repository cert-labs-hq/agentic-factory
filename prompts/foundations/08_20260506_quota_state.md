# Task: quality assurance enhacements

## Golder rule
* **Understand**: Ensure getting right the instructions. Warn any ambigueti and abort any output until it's clear what to do

## Pre-flight Checklist (Mandatory)
1. **Refresh Context:** Read the current contents of `GEMINI.md`, `docs/master-spec.md`
2. **Check Status:** Ensure no conflicting manual changes have been made since the last turn.

## Requirements
- I need to verify create a new record on the `telemetry.json` file. This one is to get the subscription quota remaining.

```json
"quota_governance": {
  "daily_limit": 500000,
  "used_24h": 221950,
  "used_percentaje": "55%",
  "remaining": 278050,
  "remaining_percentage": "35%",
  "hours_to_reset": "4 hours"
}
```

## Output
- **Implementation:** Make neccesary changes in `docs/master-spec.md` or `docs/specs/overview.md` files to add this new quota concept. Force to add the quota information update with the telemetry token utilization info.
- **Telemetry Entry:** Modify the `telemetry.json` file with this new information. 
- **Modify skill** Modify the telemetry-analyst to always udate this new information.
- **Update telemetry** After all changes and telemetry updates run the /update-telemetry command to perform a full telemetry update. This time making the new slice aggrefation.
- **Token Receipt:** A breakdown of this interaction (Input, Reasoning, Output).
